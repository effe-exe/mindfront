[
 {
  "type": "function",
  "function": {
   "name": "expand",
   "description": "Claim adjacent unclaimed land: 16/20/24 troops per plains/highland/mountain tile, no malus, survivors return when the front runs out.",
   "parameters": {
    "type": "object",
    "properties": {
     "ratio": {
      "type": "number"
     }
    }
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "attack",
   "description": "Send troops at a bordering player.",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     },
     "ratio": {
      "type": "number"
     }
    },
    "required": [
     "target"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "boat",
   "description": "Amphibious assault on any visible player whose sharesSea is true (observe.reachableByBoat lists the nearest 10 you do not border; a neighbour works too, for ...",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     },
     "ratio": {
      "type": "number"
     }
    },
    "required": [
     "target"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "ally",
   "description": "Offer an alliance to any visible id (not only neighbors).",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     }
    },
    "required": [
     "target"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "accept_alliance",
   "description": "Accept a pending alliance request.",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     }
    },
    "required": [
     "target"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "reject_alliance",
   "description": "Reject a pending alliance request.",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     }
    },
    "required": [
     "target"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "break_alliance",
   "description": "Break an alliance.",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     }
    },
    "required": [
     "target"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "extend_alliance",
   "description": "Ask an ally to renew: when both sides have called this, the alliance resets to a full 5 minutes from that moment (no time window; tribes always agree within ...",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     }
    },
    "required": [
     "target"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "donate",
   "description": "Give an ally troops OR gold (one of them, a whole number; a 0 or 1 in the other field is ignored).",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     },
     "troops": {
      "type": "integer",
      "exclusiveMinimum": 0
     },
     "gold": {
      "type": "integer",
      "exclusiveMinimum": 0
     }
    },
    "required": [
     "target"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "recall_boats",
   "description": "Turn your boats around: every transport at sea, or only those sailing at target.",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     }
    }
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "embargo",
   "description": "Stop trading with a player (stop: true lifts it).",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     },
     "stop": {
      "type": "boolean"
     }
    },
    "required": [
     "target"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "move_warship",
   "description": "Move a Warship's patrol point to water coordinates (x,y) on the same sea.",
   "parameters": {
    "type": "object",
    "properties": {
     "id": {
      "type": "integer"
     },
     "x": {
      "type": "integer"
     },
     "y": {
      "type": "integer"
     }
    },
    "required": [
     "id",
     "x",
     "y"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "build",
   "description": "Build a structure with gold.",
   "parameters": {
    "type": "object",
    "properties": {
     "unit": {
      "type": "string",
      "enum": [
       "City",
       "Port",
       "Defense Post",
       "Missile Silo",
       "SAM Launcher",
       "Factory",
       "Warship"
      ]
     },
     "at": {
      "anyOf": [
       {
        "type": "integer"
       },
       {
        "type": "string",
        "const": "sea"
       }
      ]
     }
    },
    "required": [
     "unit"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "upgrade",
   "description": "Raise one of your structures a level in place: instant, costs the same as the next new one of that type (the shared price ladder advances), no 15-tile spacin...",
   "parameters": {
    "type": "object",
    "properties": {
     "unit": {
      "type": "string",
      "enum": [
       "City",
       "Port",
       "Defense Post",
       "Missile Silo",
       "SAM Launcher",
       "Factory",
       "Warship"
      ]
     },
     "id": {
      "type": "integer"
     }
    },
    "required": [
     "unit"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "retreat",
   "description": "Stop-loss: cancel your running attacks.",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     }
    }
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "nuke",
   "description": "Launch a warhead from one of your Missile Silos at a player's territory.",
   "parameters": {
    "type": "object",
    "properties": {
     "target": {
      "type": "integer"
     },
     "nuke": {
      "type": "string",
      "enum": [
       "Atom Bomb",
       "Hydrogen Bomb",
       "MIRV"
      ]
     }
    },
    "required": [
     "target",
     "nuke"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "emoji",
   "description": "Send an emoji to one player, or to everyone when target is omitted.",
   "parameters": {
    "type": "object",
    "properties": {
     "emoji": {
      "type": "string"
     },
     "target": {
      "type": "integer"
     }
    },
    "required": [
     "emoji"
    ]
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "chat",
   "description": "Send a quick-chat message to one player; it lands in their recentEvents, no game effect.",
   "parameters": {
    "type": "object",
    "properties": {
     "key": {
      "type": "string"
     },
     "target": {
      "type": "integer"
     }
    },
    "required": [
     "key",
     "target"
    ]
   }
  }
 }
]
